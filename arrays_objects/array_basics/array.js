// Array literal

const numbers = [5,62,3,5];
const mixed = [5,"New",true];

// Array Constructor :-

const fruits = new Array("Apple","Grapes",);

// x = numbers.length;

numbers[numbers.length] = 89 // To add new value in the last use (var.length)

fruits[0] = "Grapes";
 
console.log(numbers);