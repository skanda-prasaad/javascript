// Ways to declare a variable
// var,const,let

let firstName = "John";
let lastName = "alex";
console.log(firstName, lastName, age);
var age = 30;
// we can't log 'let' before initilizing but we can in using 'var'(undefined).
 
// using push() to add in array
const arr = [1,2,6,2];
arr.push(8);
console.log(arr);
